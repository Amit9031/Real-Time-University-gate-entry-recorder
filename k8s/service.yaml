apiVersion: v1
kind: Service
metadata:
  name: gate-entry-service
  labels:
    app: gate-entry
spec:
  type: NodePort
  selector:
    app: gate-entry
  ports:
    - name: http
      port: 9090
      targetPort: 9090
      nodePort: 30090


